class HandoffAgent:
    """
    Agente encargado de escalar la conversación
    a un operador humano.
    """

    def execute(self, message: str) -> str:

        return (
            "Tu consulta será derivada "
            "a un operador humano."
        )